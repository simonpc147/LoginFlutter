import 'package:flutter/material.dart';

class MailValidationPage extends StatelessWidget {
  const MailValidationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Validacion de Correo'),
      ),
    );
  }
}
