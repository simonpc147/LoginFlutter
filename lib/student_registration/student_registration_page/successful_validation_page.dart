import 'package:flutter/material.dart';

class SuccessfulValidationPage extends StatelessWidget {
  const SuccessfulValidationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Vista de Validacion'),
      ),
    );
  }
}
